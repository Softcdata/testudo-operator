---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backuprestorestatisticses.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: BackupRestoreStatistics
    listKind: BackupRestoreStatisticsList
    plural: backuprestorestatisticses
    singular: backuprestorestatistics
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scopeType
      name: ScopeType
      type: string
    - jsonPath: .status.statistics.total
      name: Total
      type: integer
    - jsonPath: .status.statistics.inProgress
      name: InProgress
      type: integer
    - jsonPath: .status.statistics.completed
      name: Completed
      type: integer
    - jsonPath: .status.statistics.failed
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: BackupRestoreStatistics is the Schema for the backuprestorestatistics
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupRestoreStatisticsSpec defines the desired state of
              BackupRestoreStatistics
            properties:
              scopeRef:
                description: ScopeRef defines the reference to the scope object
                properties:
                  apiVersion:
                    description: APIVersion of the referent.
                    type: string
                  kind:
                    description: Kind of the referent.
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: Namespace of the referent.
                    type: string
                  uid:
                    description: UID of the referent.
                    type: string
                type: object
              scopeType:
                description: ScopeType defines the scope of the statistics
                enum:
                - app
                - namespace
                - cluster
                - custom
                type: string
            required:
            - scopeRef
            - scopeType
            type: object
          status:
            description: BackupRestoreStatisticsStatus defines the observed state
              of BackupRestoreStatistics
            properties:
              associatedResources:
                description: AssociatedResources is a list of resources associated
                  with the statistics
                items:
                  description: AssociatedResource defines a resource associated with
                    the statistics
                  properties:
                    apiVersion:
                      description: APIVersion of the referent.
                      type: string
                    kind:
                      description: Kind of the referent.
                      type: string
                    name:
                      description: Name of the referent.
                      type: string
                    namespace:
                      description: Namespace of the referent.
                      type: string
                    role:
                      description: Role of the resource (e.g., "backup", "restore")
                      type: string
                    uid:
                      description: UID of the referent.
                      type: string
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eventLog:
                description: EventLog is a log of recent events (capped at 100)
                items:
                  description: StatisticsEvent defines an event in the statistics
                    history
                  properties:
                    changes:
                      additionalProperties:
                        format: int32
                        type: integer
                      description: Changes applied in this event
                      type: object
                    message:
                      description: Message describing the event
                      type: string
                    reason:
                      description: Reason for the event
                      type: string
                    timestamp:
                      description: Timestamp of the event
                      format: date-time
                      type: string
                    type:
                      description: Type of the event (e.g., "Increment", "Decrement",
                        "Transition", "Sync")
                      type: string
                  required:
                  - timestamp
                  - type
                  type: object
                type: array
              lastUpdateReason:
                description: LastUpdateReason is the reason for the last update
                type: string
              lastUpdateTime:
                description: LastUpdateTime is the last time the statistics were updated
                format: date-time
                type: string
              statistics:
                description: Statistics contains the counters
                properties:
                  canceled:
                    default: 0
                    description: Canceled number of operations
                    format: int32
                    type: integer
                  completed:
                    default: 0
                    description: Completed number of operations
                    format: int32
                    type: integer
                  failed:
                    default: 0
                    description: Failed number of operations
                    format: int32
                    type: integer
                  inProgress:
                    default: 0
                    description: InProgress number of operations
                    format: int32
                    type: integer
                  total:
                    default: 0
                    description: Total number of operations
                    format: int32
                    type: integer
                  unknown:
                    default: 0
                    description: Unknown number of operations
                    format: int32
                    type: integer
                required:
                - canceled
                - completed
                - failed
                - inProgress
                - total
                - unknown
                type: object
            required:
            - statistics
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
