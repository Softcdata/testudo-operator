---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: operatorruntimeconfigs.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: OperatorRuntimeConfig
    listKind: OperatorRuntimeConfigList
    plural: operatorruntimeconfigs
    shortNames:
    - orc
    singular: operatorruntimeconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Invalid')].status
      name: Invalid
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              OperatorRuntimeConfigSpec defines hot-reloadable operator runtime knobs.
              Range validation is intentionally handled by the controller so semantically
              invalid objects can be persisted, rejected from activation, and reflected in status.
            properties:
              backupRuntime:
                properties:
                  inProgressMaxWait:
                    type: string
                  pollInterval:
                    type: string
                  unknownMaxWait:
                    type: string
                type: object
              clusterRuntime:
                properties:
                  deletionRetryInterval:
                    type: string
                  reconcileInterval:
                    type: string
                  veleroInstallTimeout:
                    type: string
                  veleroZombieLockThreshold:
                    type: string
                type: object
              instanceRuntime:
                properties:
                  failedRequeue:
                    type: string
                  initializingRequeue:
                    type: string
                  minTransitionWatchdogTimeout:
                    type: string
                  steadyRequeue:
                    type: string
                  transitionWatchdogTimeout:
                    type: string
                type: object
              operationRuntime:
                properties:
                  defaultRetryInterval:
                    type: string
                  defaultTimeoutMinutes:
                    format: int32
                    type: integer
                  stepRunningRequeue:
                    type: string
                  stepStartRequeue:
                    type: string
                type: object
              restoreRuntime:
                properties:
                  emptyStatusGrace:
                    type: string
                  inProgressMaxWait:
                    type: string
                  inProgressPollInterval:
                    type: string
                  missingGrace:
                    type: string
                  podVolumeRestorePendingMaxWait:
                    type: string
                  progressCompleteGrace:
                    type: string
                  retryBackoff:
                    type: string
                  retryLimit:
                    format: int32
                    type: integer
                  retryLimitEmpty:
                    format: int32
                    type: integer
                  retryLimitMissing:
                    format: int32
                    type: integer
                  retryLimitProgress:
                    format: int32
                    type: integer
                  retryLimitStartup:
                    format: int32
                    type: integer
                  startupGrace:
                    type: string
                  unknownMaxWait:
                    type: string
                  unknownPollInterval:
                    type: string
                type: object
              storageRuntime:
                properties:
                  requeueInterval:
                    type: string
                type: object
              syncRuntime:
                properties:
                  backupInProgressRequeue:
                    type: string
                  backupObserveRequeue:
                    type: string
                  historyMissingRequeue:
                    type: string
                  historyRetention:
                    format: int32
                    type: integer
                  restoreObserveRequeue:
                    type: string
                  schedulerUpdateTimeout:
                    type: string
                type: object
            type: object
          status:
            properties:
              activeGeneration:
                format: int64
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastActivatedTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
