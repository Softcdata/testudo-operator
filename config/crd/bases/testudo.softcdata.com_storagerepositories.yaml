---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: storagerepositories.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: StorageRepository
    listKind: StorageRepositoryList
    plural: storagerepositories
    singular: storagerepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: StorageRepository Status
      jsonPath: .status.status
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: StorageRepository is the Schema for the storagerepositories API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StorageRepository
            properties:
              accessKey:
                type: string
              addressingStyle:
                default: PathStyle
                description: AddressingStyle controls the S3 bucket addressing mode
                  used by validation and runtime.
                enum:
                - PathStyle
                - VirtualHostedStyle
                type: string
              bucket:
                type: string
              caSecretRef:
                description: CASecretRef references a Secret in the same namespace
                  whose `ca.crt` entry contains a custom CA bundle.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              endpoint:
                type: string
              quotaBytes:
                description: QuotaBytes defines the storage limit
                format: int64
                type: integer
              region:
                type: string
              secretKey:
                type: string
              storageType:
                type: string
            required:
            - accessKey
            - bucket
            - endpoint
            - region
            - secretKey
            - storageType
            type: object
          status:
            description: status defines the observed state of StorageRepository
            properties:
              lastCheckTime:
                description: LastCheckTime records the time when the storage was last
                  checked
                format: date-time
                type: string
              lastEventPhase:
                description: LastEventPhase records the last event phase emitted,
                  used to avoid duplicate events
                type: string
              message:
                description: Message provides a detailed message about the current
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration records the generation that was last
                  processed
                format: int64
                type: integer
              observedMetadataHash:
                description: ObservedMetadataHash records the hash of the metadata
                  (labels/annotations) that was last processed
                type: string
              readyTimestamp:
                description: ReadyTimestamp records when the storage became Available,
                  used for Duration calculation
                format: date-time
                type: string
              reason:
                description: Reason indicates the reason for the current status
                type: string
              status:
                type: string
              totalBackupCount:
                description: TotalBackupCount records the physical backup objects
                  count inside S3 based on backups/ prefix
                format: int64
                type: integer
              usedSpaceBytes:
                description: UsedSpaceBytes records the total bytes used by backup
                  objects in the storage repository
                format: int64
                type: integer
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
