apiVersion: testudo.softcdata.com/v1
kind: DisasterOperation
metadata:
  name: timeout-op
spec:
  instanceName: timeout-instance
  operationType: failover
  timeoutMinutes: 1  # Should be optional, but good to set explicitly or rely on inheritance
