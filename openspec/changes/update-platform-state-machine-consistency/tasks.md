## 1. 方案与规范
- [ ] 1.1 完成状态机资产盘点（Operator/Server/Web）并固化状态字段所有权表
- [ ] 1.2 新增 `state-machines` 规范并通过 `openspec validate --strict`
- [ ] 1.3 同步补充 `global-events` 规范中的状态迁移语义
- [ ] 1.4 产出“平台状态机矩阵”文档（资源、状态集、允许迁移、写入者、事件映射）

## 2. P1：Operator 单写者与终态语义收敛
- [ ] 2.1 收敛 `DisasterInstance.status.fsmState` 写入边界，移除跨控制器直接写终态的竞争路径
- [ ] 2.2 为 `DisasterInstance` 增加状态迁移守卫（非法迁移拦截 + reason/message 保底）
- [ ] 2.3 修正 `AppRestore` 部分失败终态语义（状态、统计、事件一致）
- [ ] 2.4 修正 `AppBackup` Failed 非粘性问题（仅在明确触发条件下允许离开 Failed）

## 3. P1：同步失败策略显式化
- [ ] 3.1 为 DataSync/ResourceSync 定义失败后自动重试策略（默认兼容）
- [ ] 3.2 在调谐逻辑中按策略执行“继续调度 / 停止调度”
- [ ] 3.3 在状态与事件中暴露当前策略决策（便于诊断）

## 4. P2：Server/Web 语义对齐
- [ ] 4.1 对齐 Server 的实例/容灾组派生状态映射，消除与 Operator 含义漂移
- [ ] 4.2 对齐 Web 操作门禁判定，避免对混合态误放行或误禁用
- [ ] 4.3 回归 `watch` 场景，确认状态串流与列表查询语义一致

## 5. 测试与回归
- [ ] 5.1 增加状态迁移矩阵测试（重点覆盖 Instance/Operation/AppRestore/AppBackup）
- [ ] 5.2 增加跨控制器并发冲突回归（确保不会互相覆盖状态）
- [ ] 5.3 增加同步失败策略测试（继续调度/停止调度双分支）
- [ ] 5.4 增加 Server 映射契约测试与 Web 门禁回归步骤

## 6. 发布与验收
- [ ] 6.1 形成“修复前后对比”报告（状态一致性、错误收敛时延、误判率）
- [ ] 6.2 完成灰度验证与回滚预案
- [ ] 6.3 提案批准后进入实施，实施完成后归档变更
